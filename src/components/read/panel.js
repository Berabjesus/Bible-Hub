import React from 'react'
import panelCss from './panel.module.css'

const Panel = () => {
  return (
    <div className={`${panelCss.bottom}`}>
      <button>test</button>
    </div>
  )
}

export default Panel